#include <stdint.h>

// AVR
#include <avr/io.h>

int main(void)
{
    /* Initialize the system */
    
    /* Main loop */
    while (1) {

    }

    /* Satisfy compiler. Should never get here */
    return 0;
}
